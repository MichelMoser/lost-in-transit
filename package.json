{
  "name": "transit-reachability",
  "private": true,
  "type": "module",
  "scripts": {
    "build:snapshot": "node scripts/build-snapshot.mjs",
    "build:road-network": "node --max-old-space-size=8192 scripts/build-road-network.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  }
}
